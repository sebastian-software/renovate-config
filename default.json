{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Shared Renovate preset for all Sebastian Software repositories.",
  "extends": [
    "config:recommended",
    ":timezone(Europe/Berlin)"
  ],
  "labels": ["dependencies"],
  "minimumReleaseAge": "1 day",
  "internalChecksFilter": "strict",
  "packageRules": [
    {
      "description": "Release-relevant dependency updates use fix(deps) so release-please publishes deployable application images after merged dependency PRs. This intentionally covers runtime packages plus build, CI, package-manager, Node, Dockerfile and lockfile-affecting updates; purely mechanical standards rollouts live in standards.json and keep their separate chore: standards flow.",
      "matchManagers": ["npm", "github-actions", "dockerfile", "nvm"],
      "semanticCommitType": "fix"
    },
    {
      "description": "Internal scopes — trusted first-party publishers; the npm release-age cooldown would only delay our own releases without a security benefit.",
      "matchDatasources": ["npm"],
      "matchPackageNames": ["@palamedes/*", "@sebastian-gmbh/*", "@sebastian-software/*"],
      "minimumReleaseAge": null
    },
    {
      "description": "Automerge our own packages once CI is green — releases roll out org-wide without manual review.",
      "matchPackageNames": ["eslint-config-setup", "ardo", "ardo-*"],
      "automerge": true
    },
    {
      "description": "Group the OXC toolchain (oxlint, oxfmt and native bindings) into a single PR.",
      "groupName": "OXC toolchain",
      "matchPackageNames": ["oxlint", "oxfmt", "@oxlint/**", "@oxfmt/**"]
    },
    {
      "description": "Group all @palamedes/* packages into a single PR — they are released in lockstep and bind to a module-singleton runtime, so a partial bump (e.g. @palamedes/react without @palamedes/runtime) duplicates the runtime instance and breaks SSR with 'No active server i18n instance'.",
      "groupName": "Palamedes",
      "matchPackageNames": ["@palamedes/*"]
    },
    {
      "description": "Group the React core packages into a single PR — react, react-dom, react-is, react-test-renderer and react-server-dom-* share one version line and must move in lockstep, because React throws 'Incompatible React versions' on a mismatch. This is a permanent name-based guard that does not depend on source URLs or on the Renovate worker's bundled monorepo data: the React repository moved from facebook/react to react/react, and Renovate before 43.277.1 no longer groups it via group:monorepos. groupName and update types mirror upstream's group so a worker upgrade does not churn branches.",
      "groupName": "react monorepo",
      "matchUpdateTypes": ["digest", "patch", "minor", "major"],
      "matchPackageNames": ["react", "react-dom", "react-is", "react-test-renderer", "react-server-dom-*"]
    },
    {
      "description": "Automerge non-major devDependency updates once CI is green.",
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    }
  ]
}
