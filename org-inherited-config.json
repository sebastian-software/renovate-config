{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Org-level inherited config — applies the shared preset to every repository without per-repo onboarding.",
  "extends": ["github>sebastian-software/renovate-config"],
  "onboarding": false,
  "requireConfig": "optional"
}
